{
  "name": "SpongeBob",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf dist && parcel build src/index.html --no-minify --public-url ./"
  },
  "dependencies": {
    "parcel": "^2.0.0-beta.2",
    "parcel-bundler": "^1.12.5"
  }
}
